[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "insights-core-messaging"
dynamic = ["version"]
readme = {file = "../../README.md", content-type = "text/markdown"}
maintainers = [
    {name="CCX Processing Team", email="ccx-core-processing-team@redhat.com"}
]
classifiers = [
    "Intended Audience :: Developers",
    "Natural Language :: English",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14"
]

requires-python = ">= 3.11"
dependencies = [
    "insights-core-messaging-base",
    "insights-core-messaging-kafka",
    "insights-core-messaging-http",
    "pika",
    "s3fs",
]

license = "Apache-2.0"

[project.urls]
Homepage = "https://github.com/RedHatInsights/insights-core-messaging"

[tool.hatch.version]
source = "vcs"

[tool.hatch.version.raw-options]
root = "../.."

[tool.hatch.build.targets.wheel]
only-include = ["_"]

[tool.hatch.build.targets.wheel.force-include]
"../../insights_messaging/consumers/rabbitmq.py" = "insights_messaging/consumers/rabbitmq.py"
"../../insights_messaging/publishers/rabbitmq.py" = "insights_messaging/publishers/rabbitmq.py"
"../../insights_messaging/requeuers/rabbitmq.py" = "insights_messaging/requeuers/rabbitmq.py"
"../../insights_messaging/downloaders/s3.py" = "insights_messaging/downloaders/s3.py"
