# This workflow is synced from RedHatInsights/processing-tools automatically; do not edit in downstream repos.

name: Linters

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  lint:
    uses: RedHatInsights/processing-tools/.github/workflows/linters.yaml@v0.3.1
